{
  "name": "incident-investigator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy --env production",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply incident-investigator --local",
    "db:verify:local": "wrangler d1 execute incident-investigator --local --command \"SELECT name FROM sqlite_schema WHERE type = 'table' AND name IN ('incidents', 'messages', 'tool_runs', 'reports') ORDER BY name;\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.17.0",
    "@cloudflare/workers-types": "^5.20260920.1",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.4",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
